<UserControl x:Class="MasterAngler.View.ControlView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        mc:Ignorable="d ignore"
        DataContext="{Binding Control, Source={StaticResource Locator}}">

    <StackPanel Orientation="Horizontal">
        <ToggleButton Content="Start" IsEnabled="{Binding IsStartEnabled}" IsChecked="{Binding IsRunning}" Width="60"/>
        <Label VerticalContentAlignment="Bottom" Content="{Binding Message}"/>
    </StackPanel>
</UserControl>
