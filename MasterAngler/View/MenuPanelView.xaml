<UserControl x:Class="MasterAngler.View.MenuPanelViewModel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        mc:Ignorable="d ignore"
        DataContext="{Binding MenuPanel, Source={StaticResource Locator}}">

    <Grid>
        <ListBox Background="Aqua" SelectedValue="{Binding SelectedItem}" SelectedValuePath="Content" SelectedIndex="0" >
            <ListBoxItem Content="Statistics"/>
            <ListBoxItem Content="Hotkeys"/>
            <ListBoxItem Content="Options"/>
        </ListBox>
    </Grid>
</UserControl>
